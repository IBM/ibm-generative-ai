# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python Build, Lint, Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:


concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11"]
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install -e ".[dev]" --disable-pip-version-check
        python -m pip install -e ".[all]" --disable-pip-version-check
    - name: Lint with pre-commit
      run: pre-commit run --all-files
    - name: Tests (unit / integration / extension)
      run: pytest -m 'unit or integration or extension' --cov-report=lcov:coverage/lcov.info --cov=genai
      env:
        GENAI_API: ${{ vars.DUMMY_GENAI_API }}
        GENAI_KEY: ${{ vars.DUMMY_GENAI_KEY }}
    - name: Coveralls
      uses: coverallsapp/github-action@v2
